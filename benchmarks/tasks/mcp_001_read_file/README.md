# Sample project

This is a tiny project used by Coco benchmark task `mcp_001_read_file`.

The one-sentence description is: **Coco is a lightweight terminal AI coding assistant.**

## Structure

- `src/` — source code
- `tests/` — unit tests
